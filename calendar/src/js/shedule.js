
/*
//first day name in month;
let firstDayName = new Date(dataYear,dataMonth, 1);
let startDayName = firstDayName.getDay();

// display table with days
//table
let tableCalendar = document.createElement('table');
//how many rows week need??
let day = 0;
//weeks in the month
const weeks = 8;
let tbody = document.createElement('tbody');
for(let d = 0; d < dataDayName;d++){
    let tr = document.createElement('tr');
    let td = document.createElement('td');
    tr.appendChild(td);
    tbody.appendChild(tr);

}
    let tr = document.createElement('tr');
    for( let j = 0; j < 7; j++ ){
        if( day < dataDays && ( i >0 || j >= startDayName)){
            day++;
            let td = document.createElement('td');
            td.appendChild(document.createTextNode(`${day}`));
            tr.appendChild(td);
            }
        }
        tbody.appendChild(tr);
    if(day > dataDays){
        break;
    }

tableCalendar.appendChild(tbody);

*/
